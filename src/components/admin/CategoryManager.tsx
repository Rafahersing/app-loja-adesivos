// src/components/admin/CategoryManager.tsx (Teste Mínimo)

import React from 'react';

const CategoryManager: React.FC = () => {
  return (
    <div style={{ padding: '20px', border: '1px solid red' }}>
      <h2>TESTE DE RENDERIZAÇÃO ESTRUTURAL (SUCCESS)</h2>
      <p>Se você vir este texto, o erro está na lógica de Supabase/useState/useEffect.</p>
    </div>
  );
};

export default CategoryManager;
